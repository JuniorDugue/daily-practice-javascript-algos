let swap = "lambda school";

const newSwap = swap.split(" ");
console.log(newSwap);

function nameSwap(str) {
  const stringToArray = str.split(" ");

  const reverseArray = stringToArray.reverse();

  console.log(reverseArray("lambda school"));
}

const school = ["lambda", "school"];
school.reverse();

console.log(school);

//
